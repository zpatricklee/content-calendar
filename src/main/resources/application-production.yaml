spring:
  datasource:
    url: "jdbc:${DATABASE_URL}"
    username: ${PGUSER}
    password: ${PGPASSWORD}
    hikari:
      data-source-properties:
        sslmode: require

  sql:
    init:
      mode: always