# PRODUCTION FILE LOADED
spring:
  datasource:
    url: "jdbc:${DATABASE_URL}"
    username: ${PGUSER}
    password: ${PGPASSWORD}
    hikari:
      data-source-properties:
        sslmode: require

  sql:
    init:
      mode: always

logging:
  level:
    org.springframework.boot.autoconfigure.jdbc: DEBUG